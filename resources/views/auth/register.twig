<!doctype html>
<html>
<head>
	<meta charset="UTF-8">
	<title>Geidni Tennis</title>
	<meta name="viewport" content="width=device-width, initial-scale=1">
	<link rel="stylesheet" type="text/css" href="/css/bootstrap.min.css">
	<link rel="stylesheet" type="text/css" href="/js/bootstrap.min.js">
	<link rel="stylesheet" type="text/css" href="/css/fontawesome.css">
	<link rel="stylesheet" type="text/css" href="/css/style.css">
	<script src="/js/jquery-3.3.1.min.js"></script>
	<script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>
	<link href='http://fonts.googleapis.com/css?family=Open+Sans:400italic,400&subset=latin,cyrillic' rel='stylesheet' type='text/css'>
</head>
<body class="main">
<div class="container-fluid">
	<div class="row">
		<div class="col-xs-12">
			<nav class="navbar navbar-default navbar-fixed-top">
				<div class="col-xs-8">

					<a class="navbar-brand" href="{{ route('main') }}">
						<img class="logo-size" src="/img/liga_grey_logo.png">
					</a>

				</div>
				<div class="col-xs-4 text-right top-padding-8px">

					<a href="{{ route('main') }}">
						<button type="button" class="lt-primary-btn">Главная</button>
					</a>

				</div>

				<!-- /.container-fluid -->
			</nav>
		</div>
	</div>
</div>


<div class="text-block text-center top-padding-for-middle">

		<div class="container">
			<form method="post" action="{{ route('register') }}">
				{{ csrf_field() }}
				<div class="row">
					<div class="col-md-6 col-sm-6">
						<div class="form-group{{ errors.has('name') ? ' has-error' : '' }}">
							<input id="name" type="text" name="name" placeholder="Имя" class="form-control" value="{{ old('name') }}"
							       required>
							{% if errors.has('name') %}
								<span class="help-block">
								<strong>{{ errors.first('name') }}</strong>
							</span>
							{% endif %}
						</div>
					</div>
					<div class="col-md-6 col-sm-6">
						<div class="form-group{{ errors.has('surname') ? ' has-error' : '' }}">
							<input id="surname" type="text" name="surname" placeholder="Фамилия" class="form-control"
							       value="{{ old('surname') }}" required maxlength="50">
							{% if errors.has('surname') %}
								<span class="help-block">
								<strong>{{ errors.first('surname') }}</strong>
							</span>
							{% endif %}
						</div>
					</div>
				</div>
				<div class="row">
					<div class="col-md-6 col-sm-6">
						<div class="form-group{{ errors.has('sname') ? ' has-error' : '' }}">
							<input id="sname" type="text" name="sname" placeholder="Отчество" class="form-control"
							       value="{{ old('sname') }}" required maxlength="50">
							{% if errors.has('sname') %}
								<span class="help-block">
								<strong>{{ errors.first('sname') }}</strong>
							</span>
							{% endif %}
						</div>
					</div>
					<div class="col-md-6 col-sm-6">
						<div class="form-group{{ errors.has('age') ? ' has-error' : '' }}">
							<input id="age" type="text" name="age" placeholder="Возраст" class="form-control" value="{{ old('age') }}"
							       required maxlength="3">
							{% if errors.has('age') %}
								<span class="help-block">
								<strong>{{ errors.first('age') }}</strong>
							</span>
							{% endif %}
						</div>
					</div>
				</div>
				<div class="row">
					<div class="col-md-6 col-sm-6">
						<div class="form-group{{ errors.has('email') ? ' has-error' : '' }}">
							<input id="email" type="email" name="email" placeholder="Email" class="form-control" value="{{ old('email') }}"
							       required maxlength="50">
							{% if errors.has('email') %}
								<span class="help-block">
								<strong>{{ errors.first('email') }}</strong>
							</span>
							{% endif %}
						</div>
					</div>
					<div class="col-md-6 col-sm-6">
						<div class="form-group{{ errors.has('phone') ? ' has-error' : '' }}">
							<input id="phone" type="text" name="phone" placeholder="Телефон" class="form-control" value="{{ old('phone') }}"
							       maxlength="50">
							{% if errors.has('phone') %}
								<span class="help-block">
								<strong>{{ errors.first('phone') }}</strong>
							</span>
							{% endif %}
						</div>
					</div>
				</div>
				<div class="row">
					<div class="col-md-6 col-sm-6">
						<div class="form-group{{ errors.has('password') ? ' has-error' : '' }}">
							<input id="password" type="password" name="password" placeholder="Пароль" class="form-control"
							       value="{{ old('password') }}" required maxlength="50">
							{% if errors.has('password') %}
								<span class="help-block">
								<strong>{{ errors.first('password') }}</strong>
							</span>
							{% endif %}
						</div>
					</div>
					<div class="form-group">
						<div class="col-md-6 col-sm-6">
							<input id="password-confirm" type="password" class="form-control" placeholder="Повторите пароль"
							       name="password_confirmation" required>
						</div>
					</div>
				</div>

				<p class="text-center text-block">
					Нажимая “Продолжить”, вы принимаете наши <a href="#">Условия и Правила</a>.
				</p>
				<div class="form-group">
					<div class="card-panel text-center text-block">
						<button type="submit" class="lt-primary-btn">
							Продолжить
						</button>
					</div>
				</div>
			</form>
		</div>
</div>
</body>
</html>